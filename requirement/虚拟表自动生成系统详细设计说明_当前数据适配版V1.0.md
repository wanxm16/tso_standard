# 面向 Text2SQL 表召回的虚拟表自动生成系统详细设计说明

> **文档版本：** V1.0（当前数据适配版）
> **文档状态：** 建议稿
> **适配对象：** `/Users/Meng/project/tso_standard/data`
> **更新日期：** 2026年4月21日

## 1. 目标重述

当前目标不是“发现已有虚拟表中的标准”，也不是“自动生成可直接执行的大宽表”，而是通过自动化方式，从现有物理表元数据、字段样例、字段使用情况和分类树中，自动生成一批专门服务于 `text2sql` 表召回的虚拟表。

这里的“虚拟表”本质上是语义召回层，而不是物理落表层。它的职责是：

1. 用更接近用户问题表达的方式组织物理表
2. 给 `text2sql` 模型提供更稳定的主题入口
3. 把自然语言问题先路由到少量候选主题表
4. 再通过字段映射回到真实物理表和真实字段

本方案的目标产物是：

1. 自动生成用于表召回的虚拟表主题
2. 自动生成用于语义理解的虚拟字段清单
3. 自动建立虚拟字段到源表字段的映射关系
4. 自动识别主语字段、时间字段、地点字段、关系字段、统计字段
5. 自动为虚拟表补充别名词、问题词和召回标签，支持查询路由
6. 用 benchmark 查询样例持续评估虚拟表是否真的提升表召回效果

一句话概括：系统的目标是“从杂乱物理表中自动沉淀面向 `text2sql` 表召回的语义虚拟表层”。

## 2. 当前输入数据画像

结合当前 `data` 目录，当前资产应区分为“生成输入”和“评估输入”两部分。

### 2.1 生成输入

1. `data/phrase_2/二期_DDL_all.csv`
2. `data/phrase_2/二期_DDL_all_with_sample.csv`
3. `data/phrase_2/二期DDL字段使用情况.csv`
4. `data/phrase_2/二期表分类树.json`

### 2.2 评估输入

1. `data/benchmark/query_with_table_1.csv`
2. `data/benchmark/query_with_table_1.json`
3. `data/benchmark/query_sql.csv`

当前数据的核心特征如下：

1. 字段总量约 10324 个
2. 物理表约 227 张
3. `STRING` 字段占比约 91.57%
4. 字段注释非空比例约 97.82%
5. 字段样例覆盖比例约 82.11%
6. benchmark 查询样例 22 条，覆盖 15 个业务模型、26 张高价值物理表

这说明当前数据非常适合做“元数据驱动的召回型虚拟表生成 + benchmark 离线评估”的方案，而不适合一开始就做复杂的运行时查询编排。

## 3. 为什么当前更适合做虚拟表自动生成

当前数据有三个很关键的条件：

### 3.1 已有较强字段语义基础

字段注释覆盖率高、字段样例覆盖率高、字段名大量使用拼音全称或稳定缩写，这意味着系统有足够证据把字段自动归一成较稳定的业务概念。

### 3.2 已有较明确的业务主题分类

`二期表分类树.json` 已经给出了较好的主题分组，这对于自动生成虚拟表主题非常关键。虚拟表不应该从“全局所有表”平地起，而应优先从分类树和物理表语义中长出来。

### 3.3 已有真实查询样例可做表召回离线评估

`query_with_table_1.csv` 和 `query_sql.csv` 提供了一个非常重要的离线评估依据：虚拟表生成之后，是否真的能承接真实问题中的主题、字段、筛选条件和输出形式。

这使得虚拟表方案可以从“拍脑袋建模”变成“先自动生成，再用真实问题做效果验证”。

## 4. 系统建设目标

当前版本的建设目标分为四层：

### 4.1 主题层

自动识别适合用于表召回的虚拟表主题，例如：

1. 人员主档
2. 流动人口
3. 轨迹事件
4. 婚姻关系
5. 亲属关系
6. 加油行为
7. 出入境记录
8. 刑监记录
9. 设备同行
10. 涉案线索

### 4.2 结构层

自动生成每个虚拟表的召回结构定义，包括：

1. 虚拟表名称
2. 主体类型
3. 表类型
4. 主语字段候选
5. 核心字段
6. 展示字段
7. 筛选字段
8. 聚合字段
9. 时间字段
10. 地点字段

### 4.3 映射层

自动建立虚拟字段与源字段的映射，包括：

1. 一对一映射
2. 多源择优映射
3. 衍生字段映射
4. 聚合字段映射

### 4.4 评估层

自动评估生成出来的虚拟表是否对 `text2sql` 表召回有帮助，包括：

1. 查询覆盖率
2. 表命中率
3. 字段覆盖率
4. SQL 重写可行率
5. 冗余虚拟表比例

## 5. 输出对象定义

系统输出的核心对象不是“标准词典”，也不是“可直接执行的逻辑视图”，而是以下五类召回资产。

### 5.1 虚拟表定义 `virtual_table`

表示一个面向业务主题的召回语义表。

字段建议包括：

1. `vt_id`
2. `vt_name`
3. `vt_cn_name`
4. `topic_name`
5. `subject_type`
6. `table_type`
7. `grain_desc`
8. `subject_key_strategy`
9. `time_semantics`
10. `recall_hints`
11. `status`

### 5.2 虚拟字段定义 `virtual_field`

表示虚拟表中的语义字段槽位。

字段建议包括：

1. `vf_id`
2. `vt_id`
3. `field_name`
4. `field_cn_name`
5. `logical_type`
6. `field_role`
7. `is_nullable`
8. `is_subject_key`
9. `is_time_field`
10. `is_geo_field`
11. `aliases`
12. `description`

### 5.3 源字段映射 `virtual_field_mapping`

描述虚拟字段从哪些源字段来、如何选、如何算，以及后续 SQL 生成应如何下钻到真实字段。

字段建议包括：

1. `vf_id`
2. `source_table`
3. `source_field`
4. `mapping_type`
5. `transform_rule`
6. `priority`
7. `confidence`
8. `mapping_scope`
9. `evidence`

### 5.4 虚拟表候选评分 `virtual_table_candidate`

描述某个主题下某组源表是否适合沉淀成虚拟表。

### 5.5 benchmark 评估结果 `virtual_table_eval`

描述虚拟表对 benchmark 查询的支持效果。

## 6. 总体架构

系统采用“主题发现 + 结构生成 + 映射生成 + 评估回路”的四段式架构。

### 6.1 主题发现层

负责识别哪些业务主题应该形成可被召回的虚拟表。

输入：

1. 分类树
2. 表中文名和表注释
3. 物理表字段分布
4. 字段使用情况

输出：

1. 虚拟表主题候选
2. 主题下候选源表集合

### 6.2 字段归一层

负责把源表字段归一成可供用户问题匹配的业务概念和逻辑类型。

输入：

1. DDL
2. 字段注释
3. 字段样例
4. 字段使用情况

输出：

1. 归一概念
2. 逻辑类型
3. 字段角色
4. 字段质量分

### 6.3 虚拟表生成层

负责生成虚拟表结构、召回标签和字段映射。

输出：

1. 虚拟表定义
2. 虚拟字段清单
3. 字段映射关系
4. 主语字段与时间字段策略
5. 表召回标签与字段别名

### 6.4 评估反馈层

负责利用 benchmark 查询验证虚拟表的表召回价值，并把结果回写到生成策略中。

## 7. 虚拟表生成的核心思想

### 7.1 虚拟表不是“分类 = 表”

不能简单把每个 L2 分类直接变成一张虚拟表。因为：

1. 有些分类更像“主档”
2. 有些分类更像“关系”
3. 有些分类更像“事件流”
4. 有些分类更像“标签或名单”
5. 有些分类内部可能需要拆成多张不同粒度的虚拟表

因此，虚拟表应以“表召回可复用粒度”为中心，而不是纯目录式分组。

### 7.2 虚拟表不是“真正合表”

当前虚拟表的定位是 `text2sql` 表召回层，因此不要求先把多个源表真实合并成一张可执行宽表。

更准确地说，虚拟表是：

1. 一个主题入口
2. 一组召回字段槽位
3. 一组别名与问题表达
4. 一组到物理表和物理字段的映射规则

因此，系统的任务不是“把所有字段并起来”，而是“把多个物理表抽象成一个更容易被用户问题命中的主题表表示”。

### 7.3 虚拟表应优先服务高频查询模式

从当前业务问题类型看，首期最值得优先支持的查询集中在以下类型：

1. 单人核查
2. 关系判断
3. 轨迹事件明细
4. 时间范围统计
5. 主题行为明细

所以虚拟表优先应围绕这些查询模式构建，而不是试图一次性覆盖所有表。

### 7.4 虚拟表的最小单元是“稳定查询粒度”

例如：

1. 人员主档表的粒度是一人一行
2. 婚姻关系表的粒度是一对关系一行
3. 轨迹事件表的粒度是一条轨迹一行
4. 加油行为表的粒度是一次加油事件一行
5. 同行关系表的粒度是一次关系事件或一对关系汇总一行

## 8. 自动生成流程

当前版本建议按如下流程生成虚拟表：

1. 解析分类树，得到初始主题集合
2. 解析物理表语义，补充主题候选
3. 以“分类主题 + 表语义 + 字段分布”融合生成候选主题
4. 对所有字段进行语义归一与逻辑类型推断
5. 在每个主题内识别候选主体表、明细表、补充表
6. 判断虚拟表类型与粒度
7. 生成核心字段集、展示字段集、筛选字段集和别名词包
8. 为每个虚拟字段选择最佳源字段或生成映射规则
9. 生成虚拟表定义草案和召回描述
10. 用 benchmark 查询对虚拟表进行离线评估
11. 根据评估结果合并、拆分、重命名或淘汰虚拟表

## 9. 主题发现策略

主题发现采用三路信号融合：

### 9.1 分类树信号

分类树提供“应该有哪几类主题”的初始边界。

### 9.2 物理表语义信号

表中文名和表注释可以帮助识别主题类型，例如：

1. `最近六个月加油信息` 明显偏事件表
2. `最后一次出入境记录` 明显偏单人最新状态表
3. `学生信息表` 偏实体主档

### 9.3 字段结构与使用信号

字段分布、关键标识、时间字段、地点字段以及字段使用情况，可以帮助判断一个主题是否应沉淀为单独虚拟表，以及应采用什么粒度。

### 9.4 融合结果

最终主题应不是简单字符串，而应包含：

1. `topic_name`
2. `topic_source`
3. `candidate_tables`
4. `topic_confidence`
5. `suggested_table_type`

## 10. 字段归一与字段角色识别

字段归一是虚拟表生成的基础。当前版本建议对每个字段识别三类信息：

1. 它表达的是什么概念
2. 它在虚拟表里扮演什么角色
3. 它是否足够重要到进入虚拟表

这里的“归一”不是把字段机械压成一个统一列名，而是把字段归入一个用户可理解的语义字段槽位。

例如：

1. `sfzh`、`gmsfhm`、`zheng_jian_hao` 可以归入 `certificate_no`
2. `xm`、`xing_ming`、`crj_xing_ming` 可以归入 `person_name`
3. `chu_ru_jing_shi_jian`、`sj`、`jin_ru_shi_jian` 可以按主题分别归入 `event_time`

同一个语义槽位下面允许存在多个来源字段映射，而不要求只有一个唯一物理来源。

### 10.1 需要识别的字段角色

建议至少识别以下角色：

1. 主语字段
2. 关系主语字段
3. 主体标识
4. 展示字段
5. 时间字段
6. 地点字段
7. 统计字段
8. 过滤字段
9. 来源字段
10. 描述字段

### 10.2 字段重要性评分

字段重要性建议综合以下因素：

1. 字段使用次数
2. 字段查询次数
3. `role_select` 权重
4. `role_where` 权重
5. `role_join` 权重
6. 字段语义清晰度
7. 字段样例质量

重要性高的字段优先进入虚拟表主字段集。

同时，字段进入虚拟表时还应额外生成：

1. 中文别名集合
2. 常见问题表达
3. 同义问法关键词

因为对表召回而言，字段能否被用户问题命中，和字段本身的别名覆盖度同样重要。

### 10.3 当前场景中的高价值逻辑类型

建议重点支持：

1. `person_name`
2. `id_card_no`
3. `passport_no`
4. `phone_no`
5. `vehicle_plate_no`
6. `datetime`
7. `region_code`
8. `address`
9. `track_point_name`
10. `relation_type`
11. `case_no`
12. `status_code`
13. `source_system`
14. `amount`
15. `count`

## 11. 虚拟表类型判定

当前版本建议优先支持五类虚拟表：

### 11.1 主档类虚拟表

适用于一人一行、一车一行、一设备一行。

示例：

1. 人员主档
2. 车辆主档
3. 地址主档

### 11.2 关系类虚拟表

适用于两主体之间的关系。

示例：

1. 婚姻关系
2. 亲属关系
3. 同行关系
4. 同监区关系

### 11.3 事件类虚拟表

适用于一次行为、一条轨迹、一条记录。

示例：

1. 出入境记录
2. 加油行为
3. 住宿记录
4. 轨迹事件

### 11.4 标签类虚拟表

适用于名单、标签、重点人员等。

### 11.5 聚合类虚拟表

适用于最近一次、近一年汇总、多源汇总等面向查询的结果层。

说明：聚合类虚拟表不建议一开始就大规模自动生成，应先按明确业务主题按需生成，再交由 benchmark 做效果验证。

## 12. 源表选择策略

不是所有进入主题的表都应该成为虚拟表的数据源。

建议对源表打分：

1. 与主题名称相似度
2. 字段完整性
3. 关键标识字段是否齐全
4. 时间字段是否齐全
5. 地点字段是否齐全
6. 数据使用热度
7. 是否存在大量无注释或技术字段

源表按角色分为：

1. 主源表
2. 补充表
3. 关系补全表
4. 统计补全表

## 13. 虚拟字段生成策略

虚拟字段生成要遵循“少而稳”的原则。

每张虚拟表的字段应优先分三层：

### 13.1 必备字段

例如：

1. 主语字段
2. 主体标识
3. 主题核心属性
4. 时间字段
5. 来源字段

### 13.2 高频展示字段

通过字段使用情况、`role_select`、`role_where` 和表注释推断。

### 13.3 可选扩展字段

用于后续增强，不作为首版主字段。

### 13.4 字段映射类型

建议支持以下映射类型：

1. 直接映射
2. 同义字段择优
3. 多字段合并
4. 字段拆分
5. 规则计算
6. 最新值提取
7. 聚合统计

说明：对 `text2sql` 表召回来说，字段映射不需要在这一层完全落成执行 SQL，只需要明确“这个虚拟字段通常应下钻到哪些真实字段”。

## 14. 主键与粒度推断

虚拟表自动生成中，最关键的是自动判断表粒度。

### 14.1 主档类表

如果表中存在身份证号、人员编号、车牌号、设备号等稳定标识，并且时间字段不是主分区语义，则倾向主档类。

### 14.2 事件类表

如果表中同时存在“主体标识 + 时间 + 地点/行为”，则倾向事件类。

### 14.3 关系类表

如果表中存在“两端主体标识 + 关系类型/关系事实”，则倾向关系类。

### 14.4 聚合类表

如果表名或注释中有“最近一次”“最后一条”“汇总”“整合”等词，且同主体只保留少量行，则倾向聚合类。

## 15. benchmark 驱动的评估机制

当前版本必须引入 benchmark 作为闭环，否则虚拟表生成容易停留在“看起来合理”。

### 15.1 评估目标

判断生成的虚拟表是否能承接 `text2sql` 表召回的核心需求。

### 15.2 建议评估指标

1. `topic_hit_rate`
2. `table_recall_at_k`
3. `virtual_field_hit_rate`
4. `query_support_rate`
5. `sql_rewrite_success_rate`

### 15.3 查询支持判定

若某条 benchmark 查询所需的：

1. 问题意图能命中某张虚拟表
2. 主过滤字段能在虚拟表中找到
3. 主输出字段能在虚拟表中找到
4. 时间条件可以表达

则认为该虚拟表对这条查询“基本可支持”。

对于表召回而言，最重要的不是“字段是否完全一致”，而是：

1. 正确主题能否进入 TopK
2. 问题中的关键实体、时间、地点是否能在虚拟表字段槽位中找到对应
3. 后续 SQL 生成是否知道应该回落到哪些物理表

## 16. 当前版本的落地策略

### 16.1 第一阶段：自动生成候选虚拟表草案

输出：

1. 主题
2. 表类型
3. 候选字段
4. 源表映射
5. 评分

### 16.2 第二阶段：人工审核少量高价值主题

优先审核：

1. 人员主档
2. 轨迹事件
3. 加油行为
4. 出入境记录
5. 婚姻关系
6. 同监区关系

### 16.3 第三阶段：用 benchmark 验证并迭代

每轮生成后重新跑 benchmark，保留提升明显的虚拟表，淘汰冗余主题。

## 17. 当前版本不建议做的事情

1. 一开始就自动生成全量 SQL 视图
2. 一开始就尝试复杂多跳 join 编排
3. 一开始就生成过宽的大宽表
4. 一开始就完全依赖 LLM 自由命名和自由建模
5. 不经过 benchmark 验证就大量产出虚拟表

## 18. 推荐的最小可行产物

如果按 MVP 落地，建议首期只交付三样东西：

1. 一份自动生成的虚拟表目录
2. 每张虚拟表的字段、别名与源表映射 JSON
3. 一份 benchmark 表召回效果报告

这样最容易判断方案是否真的有效，也最方便后续继续自动化。

## 19. 最终建议

对于当前数据，最合适的路线不是“先做标准库，再慢慢想虚拟表”，而是：

1. 以分类树和物理表语义为主题入口
2. 以字段归一、字段角色识别和字段别名扩展为底层能力
3. 以自动生成“表召回语义层”虚拟表为直接目标
4. 以 benchmark 离线评估为质量闭环

因此，当前最适合你们数据的方案，是“面向 `text2sql` 表召回的虚拟表自动生成方案”，而不是上一版偏“标准发现”的方案，也不是面向执行层的大宽表方案。
